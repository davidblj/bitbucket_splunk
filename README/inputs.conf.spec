[bitbucket_pull_requests://<name>]
*Indexa eventos de los pull requests de un repositorio en bitbucket.

# In splunk web, set the interval amount (in seconds) to: 43200 ~ 12h * 60m * 60s.
owner = <value>
repository = <value>
user = <value>
password = <value>
